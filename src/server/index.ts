/**
 * ZAP Server Module
 *
 * Re-exports the ZapServer from the main server implementation.
 */

export { ZapServer, ZapServer as Server } from '../server.js';
export type { ZapServerOptions } from '../types.js';
